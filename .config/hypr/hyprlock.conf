general {
    hide_cursor = true
    grace = 0
}

background {
    monitor =
    path = /home/example/Images/wallpapers/lock1.jpg

    # blur_size = 4
    # blur_passes = 2
}

input-field {
    monitor =
    size = 300, 60
    outline_thickness = 2
    dots_size = 0.3
    dots_spacing = 0.2
    dots_center = true
    outer_color = rgb(151515)
    inner_color = rgb(230, 230, 230)
    font_color = rgb(10, 10, 10)
    fade_on_empty = false
    placeholder_text = пупупу
    hide_input = false
    position = 0, -20
    halign = center
    valign = center
}

label {
    monitor =
	text = cmd[update:1000] echo "<b>$(date +"%H:%M:%S")</b>"
    color = rgb(250, 250, 250)
    font_size = 52
    font_family = Sans
    position = 0, 150
    halign = center
    valign = center
}
